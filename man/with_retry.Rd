% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/retry_utils.R
\name{with_retry}
\alias{with_retry}
\title{Retry Pattern with Exponential Backoff}
\usage{
with_retry(
  expr,
  max_attempts = 3L,
  backoff = 2,
  operation_name = "operation",
  silent = FALSE
)
}
\arguments{
\item{expr}{Expression to evaluate}

\item{max_attempts}{Integer maximum number of attempts (default: 3)}

\item{backoff}{Numeric base for exponential backoff in seconds (default: 2)}

\item{operation_name}{Character name of operation for logging}

\item{silent}{Logical whether to suppress retry warnings (default: FALSE)}
}
\value{
Result of expr if successful
}
\description{
Executes an expression with automatic retry logic and exponential
backoff. Useful for API calls, network operations, and external tool calls
that may fail transiently.
}
\examples{
\dontrun{
# Retry API call
result <- with_retry(
  httr2::request("https://api.example.com") |> httr2::req_perform(),
  max_attempts = 5,
  operation_name = "API call"
)

# Retry file download
with_retry(
  download.file(url, dest),
  max_attempts = 3,
  operation_name = "file download"
)
}
}
\keyword{internal}
