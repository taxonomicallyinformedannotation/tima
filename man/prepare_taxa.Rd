% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare_taxa.R
\name{prepare_taxa}
\alias{prepare_taxa}
\title{Prepare taxa}
\usage{
prepare_taxa(
  input = get_params(step = "prepare_taxa")$files$features$prepared,
  extension = get_params(step = "prepare_taxa")$names$extension,
  name_filename = get_params(step = "prepare_taxa")$names$filename,
  colname = get_params(step = "prepare_taxa")$names$taxon,
  metadata = get_params(step = "prepare_taxa")$files$metadata$raw,
  org_tax_ott = get_params(step =
    "prepare_taxa")$files$libraries$sop$merged$organisms$taxonomies$ott,
  output = get_params(step = "prepare_taxa")$files$metadata$prepared,
  taxon = get_params(step = "prepare_taxa")$organisms$taxon
)
}
\arguments{
\item{input}{\link{character} Character string path to features file with
intensities}

\item{extension}{\link{logical} Logical whether column names contain file
extensions}

\item{name_filename}{\link{character} Character string name of filename column in
metadata}

\item{colname}{\link{character} Character string name of column with biological
source info}

\item{metadata}{\link{character} Character string path to metadata file with
organism info}

\item{org_tax_ott}{\link{character} Character string path to Open Tree of Life
taxonomy file}

\item{output}{\link{character} Character string path for output file}

\item{taxon}{\link{character} Character string organism name to enforce for all
features
(e.g., "Homo sapiens"). If provided, overrides metadata-based assignment.}
}
\value{
Character string path to the prepared taxa file
}
\description{
This function prepares taxonomic information for features by
matching organism names to Open Tree of Life taxonomy. Can attribute
all features to a single organism or distribute them across multiple
organisms based on relative intensities in samples.
}
\details{
Depending on whether features are aligned between samples from
various organisms, this function either:
- Attributes all features to a single organism (if taxon specified), or
- Attributes features to multiple organisms based on their relative
intensities across samples (using metadata)
}
\examples{
\dontrun{
copy_backbone()
go_to_cache()
github <- "https://raw.githubusercontent.com/"
repo <- "taxonomicallyinformedannotation/tima-example-files/main/"
dir <- paste0(github, repo)
org_tax_ott <- paste0(
  "data/interim/libraries/",
  "sop/merged/organisms/taxonomies/ott.tsv"
)
get_file(url = paste0(dir, org_tax_ott), export = org_tax_ott)
get_file(
  url = paste0(dir, "data/interim/features/example_features.tsv"),
  export = get_params(step = "prepare_taxa")$files$features$prepared
)
prepare_taxa(
  taxon = "Homo sapiens",
  org_tax_ott = org_tax_ott
)
unlink("data", recursive = TRUE)
}
}
\seealso{
Other preparation: 
\code{\link{prepare_annotations_gnps}()},
\code{\link{prepare_annotations_mzmine}()},
\code{\link{prepare_annotations_sirius}()},
\code{\link{prepare_annotations_spectra}()},
\code{\link{prepare_features_components}()},
\code{\link{prepare_features_edges}()},
\code{\link{prepare_features_tables}()},
\code{\link{prepare_libraries_rt}()},
\code{\link{prepare_libraries_sop_bigg}()},
\code{\link{prepare_libraries_sop_closed}()},
\code{\link{prepare_libraries_sop_ecmdb}()},
\code{\link{prepare_libraries_sop_hmdb}()},
\code{\link{prepare_libraries_sop_lotus}()},
\code{\link{prepare_libraries_sop_merged}()},
\code{\link{prepare_libraries_spectra}()},
\code{\link{prepare_params}()}
}
\concept{preparation}
