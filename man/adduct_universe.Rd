% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adduct_universe.R
\name{adduct_universe}
\alias{adduct_universe}
\title{Typed adduct hypothesis system}
\description{
This module is the structured, regex-free replacement for the historical
string-concatenation logic used to enumerate adduct hypotheses and to
compute neutral masses.

Adducts are modelled as \strong{orthogonal components}:
\itemize{
\item \code{n_mer}   : oligomer multiplicity (positive integer, typically 1..3)
\item \code{carriers}: named integer vector of charge carriers (e.g. c(H = 2, Na = 1))
\item \code{clusters}: named integer vector of neutral adducts (e.g. c(H2O = 1))
\item \code{losses}  : named integer vector of neutral losses (e.g. c(H2O = 1))
\item \code{z}       : signed net charge of the resulting ion
}

The canonical adduct string is generated \strong{from} these components, never
parsed \strong{into} them inside the pipeline. All mass arithmetic happens on
the pre-computed \code{adduct_mass} offset (independent of m/z and n_mer).

Mass formula (signed, regex-free):
m/z = (n_mer * M + adduct_mass) / |z|  (with adduct_mass including
electron-mass correction: -z*m_e)
M   = (|z| * m/z - adduct_mass) / n_mer

This is physically accurate for charged ions and independent of adduct text
parsing.
}
\keyword{internal}
