% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_candidates.R
\name{rank_candidates}
\alias{rank_candidates}
\title{Rank candidates by combined evidence score}
\usage{
rank_candidates(df)
}
\arguments{
\item{df}{Data frame with columns:
\itemize{
\item feature_id
\item candidate_structure_inchikey_connectivity_layer
\item score_final (0-1, combined bio+chem+spec with weight normalization)
\item candidate_adduct_match_mode ("exact_adduct", "m_delta_rescued", "precursor_mz", or NA)
\item cluster_consensus_promoted_from_anchor (logical, TRUE if promoted)
}}
}
\value{
Data frame with added \code{rank_final} column
}
\description{
Assigns \code{rank_final} to candidates based on mathematically sound evidence weighting.

\strong{Mathematical Approach:}

Final score combines all three evidence types, automatically accounting for missing evidence
through weight normalization:

\if{html}{\out{<div class="sourceCode">}}\preformatted{score_final = (weight_bio × score_biological +
               weight_chem × score_chemical +
               weight_spec × score_spectral) / total_weight_available
}\if{html}{\out{</div>}}

Missing evidence (e.g., MS1-only hits) reduces total_weight_available, so score_final
already reflects the available evidence. No additional penalty applied.

This approach:
\itemize{
\item Uses \strong{only ONE score} for ranking (no double-counting evidence)
\item Includes \strong{all evidence types} (biological, chemical, spectral)
\item Handles \strong{missing spectral data} gracefully through weight normalization (MS1-only hits)
\item \strong{No explicit coverage penalty} (weights already reflect available evidence)
}

\strong{Ranking Logic:}

Dense rank candidates by \code{score_final} (descending):
\enumerate{
\item Primary: score_final (higher is better)
\item Tie-breaker 1: cluster_consensus_promoted (promoted first, for network consistency)
\item Tie-breaker 2: adduct_match_mode (exact > rescue > precursor)
}

Dense ranking means ties get the same rank_final, allowing multiple rank-1 candidates
when evidence is truly equivalent.

\strong{Special Cases (Rank Promotions):}
\itemize{
\item \strong{Cluster consensus promotion}: Candidates matching cross-feature consensus
are promoted to rank_final=1 as primary tie-breaker (indicates high-confidence clusters)
\item \strong{Adduct match priority}: Exact adduct matches preferred over rescue matches
(rescue = neutral mass match with adduct mismatch)
}

\strong{Why This Approach:}
\itemize{
\item Score_final already combines all evidence and accounts for missing data via weights
\item Weight normalization is mathematically sound (missing evidence reduces total_weight_available)
\item Tie-breaker hierarchy is explicit and documented
\item All special cases handled within a single, clear ranking framework
}
}
\keyword{internal}
