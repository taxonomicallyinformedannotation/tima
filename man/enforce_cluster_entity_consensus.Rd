% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_chemo_candidates.R
\name{enforce_cluster_entity_consensus}
\alias{enforce_cluster_entity_consensus}
\title{Enforce cluster-level entity consensus}
\usage{
enforce_cluster_entity_consensus(df_ranked, components_table)
}
\arguments{
\item{df_ranked}{Data frame with ranked candidates}

\item{components_table}{Data frame with feature_id and component_id columns}
}
\value{
df_ranked with cluster-consensus-enforced rankings
}
\description{
Ensures that all features within the same MS1 component/cluster
with the same neutral mass M must agree on their best InChIKey candidate.
This is a fundamental design constraint: if multiple features form an
MS1-based cluster via adduct/loss edges, they should all resolve to the
same chemical entity.

\if{html}{\out{<div class="sourceCode">}}\preformatted{For each (component_id, neutral_M) group:
- Identify the best-ranked feature by score_weighted_chemo
- Extract its best InChIKey from rank_final == 1
- All other features in the group reorder their rank=1 candidates
  such that the anchoring InChIKey becomes rank=1 (if present)
}\if{html}{\out{</div>}}
}
\keyword{internal}
