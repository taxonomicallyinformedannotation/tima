% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mztab.R
\name{.extract_embedded_mgf}
\alias{.extract_embedded_mgf}
\title{Extract embedded masster-style COM MGF spectra from mzTab-M file}
\usage{
.extract_embedded_mgf(input, features, id_col)
}
\value{
Character vector of MGF lines, or NULL if no embedded spectra found.
}
\description{
Masster encodes MS spectra inside COM lines using a custom tabular format:
\itemize{
\item \verb{COM\\tMGH\\t<col1>\\t<col2>...} defines the column header row
\item \verb{COM\\tMGF\\t<val1>\\t<val2>...} defines one spectrum per row
}
}
\details{
The function emits a \verb{FEATURE_ID=} field in each MGF entry so that
\code{\link[=get_spectra_ids]{get_spectra_ids()}} can later map spectra back to TIMA feature IDs.
Feature IDs are resolved in this priority order:
\enumerate{
\item Reverse-lookup from \code{opt_global_mgf_index} column (masster ≥ 0.5) — the
SML table stores a comma-separated list of \code{mgf_id} values per feature,
giving the authoritative bi-directional mapping.
\item Parse the \verb{id:<N>} key from the TITLE string (newer masster variants that
embed the feature/scan ID there, e.g.
\code{"id:42, rt:168.84, mz:293.1762, energy:nan, ..."})
\item Use \code{mgf_id} directly when it equals the feature ID (tools that export
a single MGF entry per feature with \code{mgf_id == SML_ID}).
}
}
\keyword{internal}
