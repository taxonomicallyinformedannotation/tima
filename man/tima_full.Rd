% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_tima.R
\name{tima_full}
\alias{tima_full}
\title{Run Complete TIMA Workflow (DEPRECATED)}
\usage{
tima_full(
  target_pattern = "^ann_wei$",
  log_file = "tima.log",
  clean_old_logs = TRUE,
  log_level = "info"
)
}
\arguments{
\item{target_pattern}{Character. Regex pattern for target selection.
Default: "^ann_wei$"}

\item{log_file}{Character. Path to log file. Default: "tima.log"}

\item{clean_old_logs}{Logical. Remove old log file before starting.
Default: TRUE}

\item{log_level}{Character or numeric. Logging verbosity level.
Default: "info"}
}
\value{
Invisible NULL (same as \code{\link{run_tima}})
}
\description{
\strong{DEPRECATED:} This function has been renamed to \code{\link{run_tima}}.
Please use \code{run_tima()} instead. \code{tima_full()} will be removed in a
future version.
}
\details{
This function is deprecated as of TIMA version 2.12.0 (November 2025).
It now simply calls \code{\link{run_tima}} with all arguments passed through,
but issues a deprecation warning.

\strong{Migration Guide:}
\itemize{
\item Old: \code{tima_full(target_pattern = "^ann_wei$")}
\item New: \code{run_tima(target_pattern = "^ann_wei$")}
}

All parameters and behavior are identical between the two functions.
}
\examples{
\dontrun{
# DEPRECATED - Use run_tima() instead
# tima_full()

# RECOMMENDED
run_tima()
}
}
\seealso{
\code{\link{run_tima}} for the current function
}
