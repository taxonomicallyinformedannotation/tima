% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adduct_universe_config.R
\name{adduct_to_string}
\alias{adduct_to_string}
\title{Build the canonical adduct string from typed components.}
\usage{
adduct_to_string(
  n_mer,
  carriers,
  clusters,
  losses,
  z,
  loss_inside_multimer = FALSE,
  cluster_inside_multimer = FALSE
)
}
\description{
Format (outside-multimer, default):
\verb{[<n>M<carriers><+clusters><-losses>]<|z|><sign>}
Format (inside-multimer, when \code{loss_inside_multimer} or
\code{cluster_inside_multimer} is \code{TRUE} and \code{n_mer >= 2}):
\verb{[<n>(M<inside-clusters><inside-losses>)<carriers><+outside-clusters><-outside-losses>]<|z|><sign>}
}
\details{
The "inside" variant captures the chemistry where each monomer carries
the cluster/loss BEFORE the multimer assembles, e.g. \verb{[2(M-H2O)+H]+}
(two M-H2O monomers dimerize, then protonate) or \verb{[2(M+NaCl)+H]+} (each
M binds NaCl first, then dimerizes). These have \emph{different} implied
neutral masses than their outside-multimer counterparts.
\itemize{
\item n omitted when n_mer == 1
\item |z| omitted when |z| == 1
}
}
\keyword{internal}
