% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adducts_utils.R
\name{canonicalize_adducts_vectorized}
\alias{canonicalize_adducts_vectorized}
\title{Canonicalize adducts with vectorized memoization}
\usage{
canonicalize_adducts_vectorized(adducts)
}
\arguments{
\item{adducts}{Character vector of adducts to canonicalize}
}
\value{
Character vector of canonicalized adducts (same length as input)
}
\description{
Fast vectorized canonicalization of many adducts, exploiting high repetition
in real MS data. Only processes each unique adduct once, then maps back
via vectorized match() + indexing.
}
\details{
Real mass-spec datasets show 95\%+ duplicate adduct values
(e.g. 278 items with only 7 unique values). This function:
\enumerate{
\item Extracts unique adducts
\item Canonicalizes each unique once (expensive operation)
\item Maps all inputs back via match() + indexing (vectorized, fast)
}

Result: 50x+ faster than naive vapply on realistic data.
}
\keyword{internal}
