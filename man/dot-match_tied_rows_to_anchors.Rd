% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_chemo_candidates.R
\name{.match_tied_rows_to_anchors}
\alias{.match_tied_rows_to_anchors}
\title{Resolve tied candidates via cross-feature neutral-mass anchors}
\usage{
.match_tied_rows_to_anchors(
  df_tied,
  anchor_tbl,
  tol,
  rt_tol,
  has_rt_feature_col
)
}
\arguments{
\item{df_tied}{Data frame with the tied candidate rows to evaluate.}

\item{anchor_tbl}{Data frame with cross-feature neutral-mass anchor rows.}

\item{tol}{Numeric mass tolerance in Daltons for matching anchors.}

\item{rt_tol}{Numeric retention-time tolerance in minutes for anchor
matching when feature RT values are available.}

\item{has_rt_feature_col}{Logical, whether the input data frames include an
RT column that can be used for anchor matching.}
}
\value{
Logical vector marking rows that have a valid cross-feature anchor.
}
\description{
Splits candidates per (feature_id, candidate_adduct,
rank_final) into untied rows (always kept) and tied rows. A tied
group is collapsed to just the candidates whose InChIKey matches
a rank-1 InChIKey of ANOTHER feature at the same neutral mass
(within tolerance). Tied groups with NO cross-feature anchor are
NOT dropped: they are kept as-is if \code{.n_per_group <= max_per_score},
otherwise sampled down to \code{max_per_score} (RT-error candidates
prioritized when the column is available).
}
\keyword{internal}
