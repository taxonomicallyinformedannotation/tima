% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adduct_universe.R
\name{generate_adduct_hypotheses}
\alias{generate_adduct_hypotheses}
\title{Generate the typed adduct universe from a structured spec for one polarity.}
\usage{
generate_adduct_hypotheses(
  spec,
  polarity,
  clusters_list = NULL,
  neutral_losses_list = NULL,
  max_clusters_per_adduct = 1L,
  max_losses_per_adduct = 1L,
  plausible_mz_range = c(50, 2000)
)
}
\arguments{
\item{spec}{list with components M (int vec), charge_carriers (list with
polarity-keyed character vectors), charges (list polarity-keyed signed
int vec). May also carry \code{clusters} (polarity-keyed character vec) and
\code{neutral_losses} (character vec); when absent these are taken from the
\code{clusters_list} / \code{neutral_losses_list} arguments.}

\item{polarity}{"pos" or "neg".}

\item{clusters_list}{optional polarity-keyed list of cluster formulas
(legacy \code{ms.clusters} fallback).}

\item{neutral_losses_list}{optional character vector of neutral-loss
formulas (legacy \code{ms.neutral_losses} fallback).}

\item{max_clusters_per_adduct}{maximum number of cluster molecules per
hypothesis (0 or 1 by default).}

\item{max_losses_per_adduct}{maximum number of neutral-loss molecules per
hypothesis (0 or 1 by default).}
}
\value{
A \code{tidytable} with one row per hypothesis and columns:
adduct, n_mer, z, adduct_mass, carriers (list), clusters (list),
losses (list), source ("structured" or "legacy").
}
\description{
Generate the typed adduct universe from a structured spec for one polarity.
}
\keyword{internal}
