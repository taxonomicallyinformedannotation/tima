% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adduct_universe.R
\name{build_adduct_universe}
\alias{build_adduct_universe}
\title{Build the typed adduct universe from a (possibly legacy) configuration.}
\usage{
build_adduct_universe(
  adducts_list,
  clusters_list,
  neutral_losses_list,
  polarity,
  max_clusters_per_adduct = 1L,
  max_losses_per_adduct = 1L,
  plausible_mz_range = c(50, 2000)
)
}
\description{
If the configuration is structured (declarative components), the universe
is enumerated systematically. If the configuration is a legacy flat list
of adduct strings, each string is parsed via \code{\link[=parse_adduct]{parse_adduct()}} and joined
to a minimal typed row that still carries the canonical adduct string and
the exact \code{adduct_mass} (so the rest of the pipeline does not care which
code path produced the row).
}
\keyword{internal}
