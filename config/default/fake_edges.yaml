#' Files.
files:

  #' Features files.
  features: 

    #' Raw features file. STRING
    raw: data/interim/features/example_feature_table.tsv.gz

  #' Networks files.
  networks:

    #' Spectral networks files.
    spectral:

      #' Edges files.
      edges:

        #' Prepared edges file. STRING
        processed: data/interim/edges/96fa7c88200e4a03bee4644e581e3fb0_faked_edges.tsv.gz

#' Names of the different variables in the input files.
names:

  #' Name of "feature IDs" variable in the input. STRING
  features: feature_id
